const size = {
  mobile: "1024px",
  desktop: "1025px",
};

const theme = {
  mainColor: "#4748C6",
  subColor: "#1B1B1B",
  mobile: `(max-width: ${size.mobile})`,
  desktop: `(min-width: ${size.desktop})`,
};

export default theme;
